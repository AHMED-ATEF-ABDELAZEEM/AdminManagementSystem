

@model List<Course>


@{
    SelectList CourseList = new(Model, "CourseId", "CourseName");
    Layout = "_MyLayout";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Department Page Body</title>
    <style>
        html, body {
            height: 100%;
            width: 100%;
            margin: 0;
        }
        .Parent {
            width:100%;
            height:100%;
            display:flex;
            justify-content:space-between;
        }

        .Sidebar {
            width:20%;
            min-height:250px;
        }

        .Sidebar a{
            text-decoration:none;
            display:block;
            padding:5px;
            margin-top:7px;
            background: linear-gradient(45deg, #1a2a6c, #b21f1f);
            border:none;
            color:#fff;
            border-radius:5px;
            min-width:140px;
        }

            .Sidebar a:hover {
                background: linear-gradient(45deg, #2a4c8c, #d12e2e);
                color: #fff;
                font-weight:bold;
            }

        .Content {
            width:65%;
            min-height:250px;
            display:flex;
        }

        #departmentSelect {
            min-width:140px;
            padding:4px 2px;
        }

    </style>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var select = document.getElementById('departmentSelect');
            var links = document.querySelectorAll('.Sidebar a');
            var contentDiv = document.querySelector('.Content');

            function updateLinks() {
                var CourseId = select.value;
                links.forEach(function (link) {
                    var baseUrl = link.getAttribute('data-base-url');
                    link.href = baseUrl + '?CourseId=' + CourseId;
                });
            }

            function loadContent(url, event) {
                event.preventDefault();
                $.ajax({
                    url: url,
                    type: 'GET',
                    success: function (result) {
                        contentDiv.innerHTML = result;
                    },
                    error: function (xhr, status, error) {
                        console.error('Error loading content:', error);
                        contentDiv.innerHTML = '<p>Error loading content. Please try again.</p>';
                    }
                });
            }

            // Update links when page loads
            updateLinks();

            // Update links when selection changes
            select.addEventListener('change', updateLinks);

            // Add click event listeners to links
            links.forEach(function (link) {
                link.addEventListener('click', function (event) {
                    loadContent(this.href, event);
                });
            });
        });
    </script>
</head>
<body>

    <div class="Parent">

        <div class="Sidebar">
            <select id="departmentSelect" asp-items="CourseList" class="form-control"></select>
            <a id="aboutLink" data-base-url="/Course/InformationAboutCourse">Information</a>
            <a id="studentLink" data-base-url="/Course/getStudentAtCourse">Students</a>
            <a id="coursesLink" data-base-url="/Course/getDepartmentThatCourseExistInIt">Departments</a>
            <a id="firstLink" data-base-url="/Course/getFirstStudentAtCourse">First</a>
            <a id="MarksLink" data-base-url="/Course/getStudentMarkAtCourse">Marks</a>
        </div>

        <div class="Content">
            @RenderBody()
        </div>

    </div>

    <script>
        $(document).ready(function () {
            $('#departmentSelect').change(function () {
                var CourseId = $(this).val();
                $.ajax({
                    url: '/Course/InformationAboutCourse',
                    type: 'GET',
                    data: { CourseId: CourseId },
                    success: function (result) {
                        $('.Content').html(result);
                    },
                    error: function (error) {
                        console.log('Error:', error);
                    }
                });
            });
        });
    </script>

</body>
</html>










